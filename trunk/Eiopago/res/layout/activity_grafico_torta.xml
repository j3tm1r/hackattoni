<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".GraficoTortaActivity" >

    <it.polito.hackattoni.visualizzazioni.VistaTorta
        android:id="@+id/graficoTorta"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="20dip" />

    <ToggleButton
        android:id="@+id/spesaProCapite"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/graficoTorta"
        android:layout_marginTop="10dp" />

    <Spinner
        android:id="@+id/spinner1"
        android:layout_width="120dip"
        android:layout_height="wrap_content"
        android:layout_alignRight="@+id/graficoTorta"
        android:layout_alignTop="@+id/spesaProCapite"
        android:layout_marginRight="10dp" />

</RelativeLayout>